node_modules/
data/
# The production SPA is checked in so a fresh checkout is immediately deployable.
# Rebuild it with `node scripts/build-frontend.js` when the frontend changes.
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
npm-debug.log*
.DS_Store
coverage/
